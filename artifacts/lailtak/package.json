{
  "name": "lailtak-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ./artifacts/api-server run dev",
    "build": "pnpm --filter ./artifacts/api-server run build",
    "start": "pnpm --filter ./artifacts/api-server run start",
    "serve": "pnpm --filter ./artifacts/api-server run start"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}